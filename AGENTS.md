# Repository guardrails

- 文書とユーザー向け表示は日本語を基本とする。
- 対象をMinecraft 26.2、NeoForge 26.2.0.59へ固定し、互換性確認なしに更新しない。
- LLMへ低レベル入力や任意コード実行を直接公開せず、許可リスト式のルーチンAPIを使う。
- 通常のサバイバルプレイヤーに不可能な観測・操作、独自パケット、reflectionを追加しない。
- すべての操作に期限、停止条件、ローカル緊急停止、監査ログを設ける。
- MCPは127.0.0.1だけにbindし、Origin検証と認証を無効化しない。
- Simple Voice Chatのミュートに失敗した場合は自動化を開始しない。
- メインのPrismインスタンスを直接変更せず、複製インスタンスで検証する。
- 特定サーバーのアドレス、認証、独自機能へ依存させず、シングルプレイでも動作させる。
- マルチプレイ固有の制約はコードへ埋め込まず、利用者が各サーバーのルールに合わせて設定できるようにする。
